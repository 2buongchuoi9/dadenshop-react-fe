// import fs from 'fs';
// import path from 'path';

// const svgFolder = './categories';

// const svgFiles = fs.readdirSync(svgFolder).filter((file) => file.endsWith('.svg'));

// const icons = [];
// svgFiles.forEach((v) => {
//   icons.push({ id: path.basename(v, '.svg'), icon: v });
// });

// export const getIcon = (id) => {
//   let icon;
//   icons.forEach((v) => {
//     if (v.id === id) {
//       icon = v.icon;
//     }
//   });
//   return icon;
// };

// export default icons;
